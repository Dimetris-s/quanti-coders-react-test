import React from "react";
import  './Form.scss'

import Input from '../UI/Input/Input'

const Form = props => {
    return (
        <form className="form">
            
        </form>
    )
}

export default Form